{
  "name": "recative-content-auth-service-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "gen-api": "openapi-typescript ../spec/reference/openapi-spec.yaml --output ./src/generated/schema.ts",
    "postinstall": "npm run gen-api",
    "test": "dotenv -e .env -- vitest --run",
    "test:update:snapshot":"dotenv -e .env -- vitest --run -u"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.14",
    "dotenv-cli": "^6.0.0",
    "openapi-typescript": "^5.2.0",
    "vitest": "^0.25.8"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "openapi-typescript-fetch": "^1.1.3"
  }
}
